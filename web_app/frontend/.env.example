# Optional. Base URL of the FastAPI backend.
# Leave empty to use the same origin (production) or the Vite dev proxy (development).
# Only set this if the backend is hosted on a different origin, e.g. http://127.0.0.1:8001
VITE_API_BASE_URL=""
